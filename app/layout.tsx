import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CAPE - Context-Aware Pseudonymization Engine",
  description:
    "Protect sensitive data through intelligent, context-aware pseudonymization. Built for the future of privacy-preserving AI.",
  verification: {
    google: "KkG3v3vCgh7YkEKCEZ365MULWt9riOVr8gn1I9_kgow",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
